#include <iostream>
using namespace std;
void CountFreq(int arr[],n)
{vector<bool>visited(n,false);
for(int i=0;i<n;i++)
{
    if(visited[i]==true)
    {
        continue;
    }
    if(count==1)
}

}
int main()
{
    int 
}