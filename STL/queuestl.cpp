#include <bits/stdc++.h>
using namespace std;
int main()
{
    //queue is the container which is just like a stack where the first in first oout concept is working
    queue<int>q;
    q.push(2);
    q.push(3);
    q.emplace(4);


}